# Hello World & Simple Values

This folder contains my first Go examples: printing output and working with simple values like strings, integers, floats, and booleans.

## What’s Inside

- `main.go`: Demonstrates `fmt.Println`/`fmt.Printf`, string literals, numeric operations, and boolean evaluation.
- Focus: Understanding the basic syntax and how Go handles simple types.

## Key Concepts

- Printing with `fmt`: `Println` (auto newline) vs `Printf` (formatted output)
- Basic types: `string`, `int`, `float64`, `bool`
- Type defaults: zero values (e.g., `0` for numbers, `""` for strings, `false` for booleans)

## Run The Example

```powershell
cd "D:\my\GUNJAN\Gunjan Go\learning\hellloworld&simplevalues"
go run main.go
```

## Notes

- Keep formatting idiomatic with `gofmt` and `go fmt`.
- Use clear variable names to make examples easy to follow.
